<?xml version="1.1" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tnta.iuwebpage" >

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logophoto"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabChange"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"></activity>
        <activity android:name=".Edusoft"></activity>
        <activity android:name=".BlackBoard"></activity>
        <activity android:name=".Email"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".EnterID"></activity>
    </application>

</manifest>
